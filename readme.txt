In this project, We will solve the problem which Mr. mituyama had.
That is gunzip so many files named "00000.gz" in each named directory.

So we should do next steps.
1. get directory names in array
2. loop each the array
3. in that loop, 
	cd ~~~
	gunzip 00000.gz
	cd ..
thats all!!!
